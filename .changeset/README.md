# Changesets

This directory holds changesets for AetherForge releases. Use:

```sh
npm run changeset
```

to record a change, then `npm run release` to bump versions.

See https://github.com/changesets/changesets for more.
